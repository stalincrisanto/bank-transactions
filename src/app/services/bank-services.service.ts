import { Injectable } from '@angular/core';
import { Transfer } from '../types/transfer';

@Injectable({
  providedIn: 'root'
})
export class BankServicesService {

  constructor() { }

  addTransfer (input: Transfer) {
    // TODO: declare service to add transfer
  }
}
